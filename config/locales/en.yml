en:
  links:
    models:
      group_session:
        add: 'Add a group session'
        edit: 'Edit this group session'
        delete: 'Cancel'
  forms:
    models:
      group_session:
        create: 'Create group session'
        edit: 'Save'
  activerecord:
    errors:
      could_not_be_saved: "Sorry, this %{model} could not be saved:"
      models:
        group_session:
          attributes:
            title:
              blank: "can't be blank"
            description:
              blank: "can't be blank"
            starts_at:
              blank: "can't be blank"
