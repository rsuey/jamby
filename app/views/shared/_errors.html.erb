<% if record.errors.any? %>
  <div class='errors'>
    <h1><%= t('errors.could_not_be_saved', model: record.class.name) %></h1>
    <ul>
      <% record.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
