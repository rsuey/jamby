<nav>
  <%= link_to t('links.models.signup.new'), signup_path %>&nbsp;|
  <%= link_to t('links.models.signin.new'), signin_path %>&nbsp;|
  <%= link_to t('links.models.signin.destroy'), signout_path, method: :delete, confirm: t('links.models.signin.confirm_logout') %>
</nav>

<div class='account-info'>
  <span class='username'><%= current_user.username %></span>
</div>
