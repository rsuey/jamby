<div><%= t('text.models.group_session.booked') %></div>
<%= link_to t('links.models.group_session.cancel_booking'),
            cancel_booking_group_session_path(group_session),
            class: 'label alert right',
            data: {
             confirm: t('controllers.group_sessions.cancel_booking.confirm')
            } %>
