<%= render 'form', button_name: t('forms.models.group_session.edit') %>

<%= link_to t('links.models.group_session.delete'), @group_session,
                                                    method: :delete, data: {
                        confirm: t('controllers.group_sessions.destroy.confirm')
                      } %>
