<%= render 'group_session', group_session: @group_session %>
<%= render 'full_description', group_session: @group_session %>
<%= render 'host_start_details', group_session: @group_session %>
<%= render 'participant_join_details', group_session: @group_session %>
<%= render 'guest_list', group_session: @group_session %>
