<% if group_session.ready_to_start? %>
  <div id='hangout_button' class='row'>
    <div class='columns small-12'>
      <%= link_to t('links.models.group_session.launch'),
                  group_session.live_url,
                  class: 'button' %>
    </div>
  </div>
<% end %>
